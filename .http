@baseUrl = http://localhost:3000

# ==========================================
# AUTH ENDPOINTS
# ==========================================

### Register a new user
# @name register
POST {{baseUrl}}/auth/register
Content-Type: application/json

{
  "name": "Admin User",
  "email": "admin@example.com",
  "password": "password123",
  "roleId": 1,
  "departmentId": 1
}

### Login user
# @name login
POST {{baseUrl}}/auth/login
Content-Type: application/json

{
  "email": "admin@example.com",
  "password": "password123"
}

### Active Token from Login Response
@authToken = {{login.response.body.token}}


# ==========================================
# USER ENDPOINTS (Requires Auth & Admin role)
# ==========================================

### Get user by ID
POST {{baseUrl}}/user/getUserById
Authorization: Bearer {{authToken}}
Content-Type: application/json

{
  "id": 7
}

### Get user by email
POST {{baseUrl}}/user/getUserByEmail
Authorization: Bearer {{authToken}}
Content-Type: application/json

{
  "email": "admin@example.com"
}

### Get users (filtered by role or department)
POST {{baseUrl}}/user/getUsers
Authorization: Bearer {{authToken}}
Content-Type: application/json

{
  "departmentId": 1
}

### Update user details
POST {{baseUrl}}/user/updateUser
Authorization: Bearer {{authToken}}
Content-Type: application/json

{
  "userId": 7,
  "data": {
    "name": "Updated Admin User"
  }
}

### Deactivate/Delete user
POST {{baseUrl}}/user/deleteUser
Authorization: Bearer {{authToken}}
Content-Type: application/json

{
  "id": 7
}

### Get all roles
GET {{baseUrl}}/user/getRoles
Authorization: Bearer {{authToken}}

### Create a new role
# Note: Defined as GET in user.route.js but reads data from req.body
GET {{baseUrl}}/user/createRole
Authorization: Bearer {{authToken}}
Content-Type: application/json

{
  "data": {
    "position": "New role"
  }
}

### Update a role
# Note: Defined as GET in user.route.js but reads data from req.body
GET {{baseUrl}}/user/updateRole
Authorization: Bearer {{authToken}}
Content-Type: application/json

{
  "id": 4,
  "data": {
    "position": "Lead Technician"
  }
}

### Delete a role
# Note: Defined as GET in user.route.js but reads data from req.body
GET {{baseUrl}}/user/deleteRole
Authorization: Bearer {{authToken}}
Content-Type: application/json

{
  "id": 4
}


# ==========================================
# ASSET ENDPOINTS (Requires Auth)
# ==========================================

### Get all assets (with pagination and filtering)
GET {{baseUrl}}/asset?page=1&limit=10&status=AVAILABLE
Authorization: Bearer {{authToken}}

### Get asset detail by ID
GET {{baseUrl}}/asset/1
Authorization: Bearer {{authToken}}

### Create a new asset (Admin only)
POST {{baseUrl}}/asset
Authorization: Bearer {{authToken}}
Content-Type: application/json

{
  "name": "MacBook Pro 16",
  "description": "Developer Laptop",
  "serialNumber": "SN-MBP16-2027",
  "category": "Laptop",
  "purchaseDate": "2026-07-28T12:00:00.000Z",
  "purchaseCost": 2499.99,
  "status": "AVAILABLE"
}

### Edit an existing asset (Admin only)
PUT {{baseUrl}}/asset/1
Authorization: Bearer {{authToken}}
Content-Type: application/json

{
  "isActive" : true
}

### Update asset status only (Admin only)
PATCH {{baseUrl}}/asset/1/status
Authorization: Bearer {{authToken}}
Content-Type: application/json

{
  "status": "AVAILABLE"
}

### Soft delete an asset (Admin only)
DELETE {{baseUrl}}/asset/1
Authorization: Bearer {{authToken}}


# ==========================================
# ASSIGNMENT ENDPOINTS (Requires Auth)
# ==========================================

### Get all assignments (with pagination and filtering)
GET {{baseUrl}}/assignment?page=1&limit=10&returnedAt=null
Authorization: Bearer {{authToken}}

### Create a new assignment (Admin only)
POST {{baseUrl}}/assignment
Authorization: Bearer {{authToken}}
Content-Type: application/json

{
  "assetId": 1,
  "employeeId": 1,
  "notes": "Assigned to team lead for development use."
}

### Return an assignment (Admin only)
PATCH {{baseUrl}}/assignment/1/return
Authorization: Bearer {{authToken}}
Content-Type: application/json

{
  "notes": "Returned in good condition with minor scratches."
}


# ==========================================
# MAINTENANCE ENDPOINTS (Requires Auth)
# ==========================================

### Create a new maintenance request (Auth required)
POST {{baseUrl}}/maintenance
Authorization: Bearer {{authToken}}
Content-Type: application/json

{
  "assetId": 1,
  "description": "Screen flickering issues on MacBook",
  "priority": "HIGH"
}

### Get all maintenance requests (Auth required)
GET {{baseUrl}}/maintenance?page=1&limit=10
Authorization: Bearer {{authToken}}

### Get maintenance request details by ID (Auth required)
GET {{baseUrl}}/maintenance/1
Authorization: Bearer {{authToken}}

### Update maintenance request status / details (Admin/Technician only)
PUT {{baseUrl}}/maintenance/1
Authorization: Bearer {{authToken}}
Content-Type: application/json

{
  "status": "IN_PROGRESS",
  "assignedTo": 1
}

### Complete maintenance request (Admin/Technician only)
PUT {{baseUrl}}/maintenance/1
Authorization: Bearer {{authToken}}
Content-Type: application/json

{
  "status": "COMPLETED",
  "cost": 150.00
}


# ==========================================
# DEPARTMENT ENDPOINTS (Requires Auth/Admin)
# ==========================================

### Create a department (Admin only)
POST {{baseUrl}}/department
Authorization: Bearer {{authToken}}
Content-Type: application/json

{
  "name": "Engineering"
}

### Get all departments
GET {{baseUrl}}/department
Authorization: Bearer {{authToken}}

### Get department by ID
GET {{baseUrl}}/department/1
Authorization: Bearer {{authToken}}

### Update department (Admin only)
PUT {{baseUrl}}/department/1
Authorization: Bearer {{authToken}}
Content-Type: application/json

{
  "name": "Software Engineering"
}

### Delete department (Admin only)
DELETE {{baseUrl}}/department/1
Authorization: Bearer {{authToken}}


# ==========================================
# ACTIVITY LOG ENDPOINTS (Requires Auth & Admin)
# ==========================================

### Get all activity logs
GET {{baseUrl}}/activity-log?page=1&limit=20
Authorization: Bearer {{authToken}}
